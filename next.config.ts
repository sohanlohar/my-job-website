import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Configure static export output so we can host the site on Firebase Hosting
  output: "export",
  // Keep existing defaults
};

export default nextConfig;
